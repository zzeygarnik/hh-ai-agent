# ZGRNK HH Agent — дизайн-мокап (вариант 1)

Экспорт из Stitch, только фронтенд-мокап с фейковыми данными (localStorage, симуляция логов через `setInterval`). Не подключён к реальному боту.

## Запуск локально
```bash
npm install
npm run dev
```
